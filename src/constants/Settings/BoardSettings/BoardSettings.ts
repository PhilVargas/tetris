import { IBoardSettings } from '../../../typings'

const BoardSettings: IBoardSettings = {
  height: 21,
  width: 10,
  hiddenRows: 1,
  yCoord: 1,
  xCoord: 4,
  rotation: 0,
  pieceIds: [],
}

export default BoardSettings
